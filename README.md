# Number-Guessing-Game.
A simple Python project where the computer randomly selects a number between 1 and 10, and the user has to guess it. The program gives hints ("Higher"/"Lower") until the user finds the correct number. It also tracks the number of attempts. It's Features such as Random number generation, User input with hints and Attempts countes.
